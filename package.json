{
  "dependencies": {
    "@types/ramda": "^0.26.15",
    "@types/socket.io": "^2.1.2",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "jest": "^24.8.0",
    "ramda": "^0.26.1",
    "socket.io": "^2.2.0",
    "socket.io-client": "^2.2.0",
    "typescript": "^3.5.2",
    "xstate": "^4.6.2"
  },
  "devDependencies": {
    "ts-node-dev": "^1.0.0-pre.40"
  },
  "scripts": {
    "tsc": "tsc",
    "dev": "NODE_ENV='development' && ts-node-dev --respawn --transpileOnly ./src/app.js",
    "prod": "NODE_ENV='production' && tsc && node ./build/app.js",
    "build-client": "cd client && yarn build",
    "test": "jest"
  }
}
